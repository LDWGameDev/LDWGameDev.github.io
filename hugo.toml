baseURL = 'https://LDWGameDev.github.io/'
languageCode = 'en-us'
title = 'Ldwork'
enableRobotsTXT = true

# Project list lives in data/projects.yaml — accessed via site.Data.projects.

# Suppress the section list page for content/page/. Only its inner files
# (e.g. project.md routed to /project/) are intended to render.
[outputs]
  section = []
